let trees = ['oak', 'palm', 'birch']
trees.forEach( tree => console.log(tree) )// is this a forEach method or function.

    
let temp = 30
let winter = temp < 32 ? true: false
console.log(temp)  //temp is 30 and winter is true because it is less then 32.